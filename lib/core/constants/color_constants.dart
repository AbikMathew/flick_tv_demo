import 'package:flutter/material.dart';

/// Color constants for the success screen
class ColorConstants {
  // Text colors
  static const Color textWhite = Colors.white;
  static const Color textBlack = Colors.black;

  // Overlay and background colors
  static Color floatingButtonBackground = Colors.black.withValues(alpha: 0.4);
  static Color floatingButtonBorder = Colors.white.withValues(alpha: 0.08);
  static Color shadowColor = Colors.black.withValues(alpha: 0.3);

  // Icon colors
  static const Color iconWhite = Colors.white;
}
